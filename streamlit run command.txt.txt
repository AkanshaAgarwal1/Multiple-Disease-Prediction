streamlit run "C:\Users\acer\multiple disease prediction\multiple disease prediction.py"


multiple disease prediction

in first command prompt:

pip install streamlit
pip install streamlit-option-menu
pip install pickles
pip install scikit-learn
pip install numpy

in second command prompt:

pip install streamlit
streamlit run "C:\Users\acer\multiple disease prediction\multiple disease prediction.py"

while filling values for each disease, after every result - refresh the page and then fill the values for next prediction. only then it will give best and correct result.

sabse pehle anaconda kholna h- fir usme machine learning wala choose karna h - uske baad environment me open terminal select karna h or fir usse command prompt khul jayega- an commands likhne h pehle first command prompt me or fir second command prompt me....dono commands alag alag cmd pt me honge run.
